apiVersion: v1
kind: Service
metadata:
  name: authelia-webui
  labels:
    app.kubernetes.io/name: authelia-webui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: authelia-webui
  ports:
    - name: http
      port: 80
      targetPort: http
